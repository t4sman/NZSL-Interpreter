import React from 'react';
import {LoginSignup} from '../components';

const loginSignup = () => {
    return (
        <div>
        <LoginSignup />
        </div>
    );
};

export default loginSignup;